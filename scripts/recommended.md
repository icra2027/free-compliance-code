Recommendation: add the actual unfreeze support before spending GPU time — a --train-expert-only/--unfreeze-vlm flag on train_policy.py (checked: lerobot's train_expert_only=False unfreezes the VLM's text tower down to the last 1-2 layers + lm_head, while freeze_vision_encoder can stay True to leave the vision side alone) plus wiring the --lr override the sbatch script already anticipates ("optional override, e.g. for an unfreeze experiment's smaller LR" — that comment exists but nothing sets it yet). Launch that on the existing data (frozen split is fine for this, no need for --all-sessions yet), then rerun diagnose_language_grounding.py per the handoff's instructions and check whether top-1 accuracy clears ~25% chance. Only reach for --all-sessions/more collection if that comes back flat.
